// import { render } from '@/config/test-utils'

// import Feedback from './page'

// -----------------------------------------
// Temp removal
// -----------------------------------------
test('page content renders correctly', async () => {
  // const { getByRole } = render(await Feedback({ searchParams: {} }))
  // expect(
  //   getByRole('heading', { level: 2, name: 'What was your reason for visiting the dashboard today?' })
  // ).toBeInTheDocument()
  // expect(getByRole('heading', { level: 2, name: 'Did you find everything you were looking for?' })).toBeInTheDocument()
  // expect(
  //   getByRole('heading', { level: 2, name: 'How could we improve your experience with the dashboard?' })
  // ).toBeInTheDocument()
  // expect(
  //   getByRole('heading', { level: 2, name: 'What would you like to see on the dashboard in the future?' })
  // ).toBeInTheDocument()
})

// test('error message displays', async () => {
//   const { getByRole, getByText } = render(await Feedback({ searchParams: { error: '1' } }))

//   expect(getByRole('heading', { level: 2, name: 'There is a problem' })).toBeInTheDocument()
//   expect(getByText('There was a problem processing the request. Please try again later.')).toBeInTheDocument()
// })
