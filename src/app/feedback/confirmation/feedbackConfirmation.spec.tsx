// import { render } from '@/config/test-utils'

// import FeedbackConfirmation from './page'

// -----------------------------------------
// Temp removal
// -----------------------------------------
test('page content renders correctly', async () => {
  // const { getByRole, getByText } = render(await FeedbackConfirmation())
  // expect(getByRole('heading', { level: 1, name: 'Form submitted' })).toBeInTheDocument()
  // expect(getByText('Thank you for your feedback')).toBeInTheDocument()
  // expect(
  //   getByText(
  //     /Thank you for giving feedback on the UKHSA data dashboard. We'll use your comments to help us continually improve the service. We will not be able to get in touch with you about your comments./
  //   )
  // ).toBeInTheDocument()
  // expect(
  //   getByText(
  //     /The feedback form is intended for feedback on your experience using the dashboard. If you need to get in touch with UK Health Security Agency, you can find contact information at the bottom of the/
  //   )
  // ).toBeInTheDocument()
  // expect(getByRole('link', { name: 'UKHSA webpage' })).toBeInTheDocument()
})
