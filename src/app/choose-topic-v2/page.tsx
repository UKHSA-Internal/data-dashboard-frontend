import { Wrapper } from '@/app/components/ui/ukhsa'

export default async function ChooseTopicPage() {
  return (
    <Wrapper heading={'heading'} description={'description'} lastUpdated={'lastUpdated'}>
      choose topic
    </Wrapper>
  )
}
