import { renderCard } from './renderCard'
import { renderChartRowColumn } from './renderChartRowColumn'
import { renderHeadlineRowColumn } from './renderHeadlineRowColumn'

export default {
  renderCard,
  renderChartRowColumn,
  renderHeadlineRowColumn,
}
