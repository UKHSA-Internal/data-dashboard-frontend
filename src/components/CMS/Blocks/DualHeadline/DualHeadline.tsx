import { CardColumn } from '@/components/Card'

interface DualHeadlineProps {
  heading: string
}

export const DualHeadline = ({ heading }: DualHeadlineProps) => {
  return <CardColumn heading={heading}>tbc</CardColumn>
}
