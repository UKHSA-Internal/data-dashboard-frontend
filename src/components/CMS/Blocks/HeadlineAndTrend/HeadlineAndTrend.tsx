import { CardColumn } from '@/components/Card'

interface HeadlineAndTrendProps {
  heading: string
}

export const HeadlineAndTrend = ({ heading }: HeadlineAndTrendProps) => {
  return <CardColumn heading={heading}>tbc</CardColumn>
}
